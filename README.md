# garrulous-octo-spork

Using `django_inbound_email` and `post_office` for inbound sendgrid and outbound sendgrid, respectively. Post_office can send on a schedule, each email has it's own send time.
